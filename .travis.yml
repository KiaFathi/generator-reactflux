language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-reactflux' ]; then cd .. && eval "mv $currentfolder generator-reactflux" && cd generator-reactflux; fi

